# Developer TODO / backlog

Engineering tasks for future contributors (not auto-generated).

- [ ] Replace `mockResearchBundle` with real APIs (transit, listings, places) and caching.
- [ ] Add structured geocoding when the user types an address hint (Google Geocoding or similar).
- [ ] Support **multiple saved pins** in readable context for comparison flows.
- [ ] Optional: stream partial `render_a2ui` progressive surfaces for long research.
- [ ] Add E2E smoke test: pin → message → tool calls → A2UI fragment renders.
- [ ] Harden error states when `GOOGLE_API_KEY` is missing (user-friendly banner).
- [ ] Review CopilotKit / AG-UI version bumps quarterly.

## Product ideas (stretch)

- “What changes if I work late?” — parameterized follow-up that swaps A2UI emphasis.
- SIWX / x402 paid enrichment hooks (if using external paid APIs).
